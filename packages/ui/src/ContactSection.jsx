import { ExternalLink, Mail, Phone } from 'lucide-react'

export function ContactSection({ content }) {
  const external = (href) => /^https?:\/\//.test(href) ? { target: '_blank', rel: 'noopener noreferrer' } : {}
  return <section id="contacto" className="scroll-mt-28 py-16 sm:py-20" aria-labelledby="titulo-contacto"><div className="mx-auto max-w-content px-5 sm:px-8"><div className="grid gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-16"><div><p className="eyebrow text-brand-primary">{content.eyebrow}</p><h2 id="titulo-contacto" className="section-title mt-3">{content.title}</h2><p className="mt-5 text-lg leading-8 text-slate-700">{content.description}</p><address className="mt-8 not-italic"><p className="font-black">{content.contact_name}</p><p className="mt-1 text-slate-600">{content.contact_role}</p><ul className="mt-5 space-y-3">{content.phones.map((phone) => <li key={phone}><a className="inline-flex min-h-11 items-center gap-3 font-bold text-brand-primary underline" href={`tel:${phone.replace(/[^+\d]/g, '')}`}><Phone aria-hidden="true" size={20}/>{phone}</a></li>)}{content.emails.map((email) => <li key={email}><a className="inline-flex min-h-11 items-center gap-3 break-all font-bold text-brand-primary underline" href={`mailto:${email}`}><Mail aria-hidden="true" size={20}/>{email}</a></li>)}</ul>{!content.phones.length && !content.emails.length && <p className="mt-5 text-slate-600">No hay medios de contacto publicados.</p>}</address></div><div><h3 className="text-2xl font-black">Otros recursos</h3>{content.resources.length ? <ul className="mt-5 divide-y divide-line border-y border-line">{content.resources.map((item) => <li key={item.key}><a className="grid min-h-28 grid-cols-[1fr_auto] items-center gap-4 py-5" href={item.href} {...external(item.href)}><span><strong className="block">{item.title}</strong><span className="mt-1 block text-sm leading-6 text-slate-600">{item.description}</span></span><ExternalLink aria-hidden="true" size={20}/></a></li>)}</ul> : <p className="mt-5 rounded-lg border border-dashed p-6">No hay recursos adicionales publicados.</p>}</div></div><section className="mt-16 border-t border-line pt-10" aria-labelledby="titulo-creditos"><p className="eyebrow text-brand-primary">Créditos del sitio original</p><h3 id="titulo-creditos" className="mt-3 text-2xl font-black">Equipo de desarrollo</h3><div className="mt-7 grid gap-8 md:grid-cols-3"><div><h4 className="font-black">Asesoría curricular</h4><p className="mt-2">Yeri Charpentier Díaz</p></div><div><h4 className="font-black">Recursos tecnológicos — GESPRO</h4><p className="mt-2">Patricia Hernández Conejo<br/>Óscar Pérez Ramírez<br/>Luis Chacón Campos</p></div><div><h4 className="font-black">Diseño gráfico</h4><p className="mt-2">Mariana Molina Rojas</p></div></div></section></div></section>
}
